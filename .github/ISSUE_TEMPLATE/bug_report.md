---
name: Bug report
about: Something in enjambre doesn't behave the way it's documented to
title: ""
labels: bug
---

**What happened**
A clear description of the bug.

**What you expected**
What should have happened instead.

**How to reproduce**
Minimal `swarm.yaml` / commands that trigger it. A failing `pytest` case is even better.

**Environment**
- enjambre version (`enjambre --version`):
- OS:
- Python version (`python --version`):

**Logs**
Relevant output, with `-v` if you can (`enjambre -v ...`).
