---
name: memory-graph
description: Notes become a graph you can explore, query and teach with
type: core
---
# memory graph

Every node is a real note and every edge a real link. Red nodes are notes that are
linked but not written yet, like [[solar-window]]: a gap you can see is a gap you
can fill. Agents query the same graph through the API and the MCP server.
Part of [[enjambre]].
